# 依赖与构建产物
.android-build/
node_modules/
dist/
build/
.gradle/

# APP/ 打包成品目录（exe + 配置 + 脚本，上传到 GitHub 供直接下载）
# 注意：APP/电脑端监控/data/ 仍被排除（含运行时密钥）
APP/电脑端监控/data/

# 本地运行时数据（含配对密钥、推送 key、自签证书，绝不上传）
data/
*.log

# 安卓本地配置与签名
local.properties
*.jks
*.keystore

# 临时文件
*.tmp
.DS_Store
Thumbs.db

# 编辑器/工具临时目录
.playwright-mcp/
.zcode/
